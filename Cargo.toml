[package]
name = "shared"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"

[dependencies]
bevy = { version = "0.18", default-features = false }
lightyear = { version = "0.26", features = ["replication"] }
serde = { version = "1", features = ["derive"] }
bitcode = "0.6"

[dev-dependencies]
serde_json = "1"
